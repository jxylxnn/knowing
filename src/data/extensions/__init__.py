"""Data-source extension scrapers.

Drop a module here that defines a concrete ``BaseScraper`` subclass and it
will be auto-discovered by ``ScraperRegistry``. Enable it via the
``data_sources`` block in ``config/default.yaml``.
"""